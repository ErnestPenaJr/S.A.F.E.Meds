import PagePlaceholder from '@/components/common/PagePlaceholder';

export default function Profile() {
  return <PagePlaceholder title="My Profile" note="Account, allergies, notifications, theme picker, contacts — Phase 1+." />;
}
