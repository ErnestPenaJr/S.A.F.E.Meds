import PagePlaceholder from '@/components/common/PagePlaceholder';

export default function SharedView() {
  return <PagePlaceholder title="Shared List" note="Public view of a shared list via access code — Phase 5." />;
}
